[alias]
    st = status
    ado = add .
    co = commit -v
    po = push origin

    br = branch
    ch = checkout
    cb = "!f () { git checkout -b $1 origin/$1;};f"

    cl = clone
    fe = fetch

    creset = reset --hard HEAD^
